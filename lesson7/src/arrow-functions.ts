//From JS file:
//export const sumArray = (arr) => arr.reduce((acc, curr) => acc + curr, 0);

export const sumArray = (arr: number[]): number => arr.reduce((acc: number, curr: number) => acc + curr, 0);
